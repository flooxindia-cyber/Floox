const {findUser}=require('../server/functions/_utils'); const {issue}=require('../server/otp'); module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const {email,purpose='registration'}=req.body||{};if(!email)return res.status(400).json({error:'Email is required.'});try{const u=await findUser('email','eq',email.toLowerCase().trim());if(!u)return res.status(404).json({error:'Account not found.'});if(purpose==='registration'&&u.verified)return res.status(400).json({error:'Account is already verified.'});await issue(u.email,u.id,purpose,u.name);return res.status(200).json({message:'A new verification code has been sent.'});}catch(e){return res.status(500).json({error:e.message||'Could not resend OTP.'});}};
