const {findUser}=require('../server/functions/_utils'); const {issue}=require('../server/otp'); module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const email=String((req.body||{}).email||'').toLowerCase().trim();if(!email)return res.status(400).json({error:'Email is required.'});try{const u=await findUser('email','eq',email);if(u)await issue(u.email,u.id,'password_reset',u.name);return res.status(200).json({message:'If that email is registered, a password reset code has been sent.'});}catch(e){console.error(e);return res.status(500).json({error:'Could not send reset code.'});}};
